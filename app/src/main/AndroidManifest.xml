<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.scout.patient">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.DoctorsProfile.DoctorsProfileActivity"></activity>
        <activity android:name=".ui.BookAmbulanceActivity" />
        <activity
            android:name=".ui.AppointmentBooking.BookAppointmentActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.DoctorsActivity.DoctorsActivity"
            android:label="@string/doctor" />
        <activity
            android:name=".ui.WelcomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.Auth.Registration.RegistrationActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.Auth.LoginActivity.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>